{#
  @file
  Banner menu template for displaying a list of links as cards.
#}
<ul class="menu menu-banner">
  {% for item in items %}
    <li class="menu-item">
      <a href="{{ item.url }}" class="menu-link">
        {{ item.title }}
      </a>
    </li>
  {% endfor %}
</ul>
